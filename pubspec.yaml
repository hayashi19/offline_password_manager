name: offline_passowrd_manager
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.1+101

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: ^4.6.1
  random_password_generator: ^0.2.1
  get_storage: ^2.0.3
  rename: ^2.0.1
  flutter_launcher_icons: ^0.12.0
  flutter_native_splash: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter_icons:
  android: "launcher_icon"
  image_path: "assets/Logo Offline Password.png"

flutter_native_splash:
  color: "#020522"
  image: assets/Logo Offline Password.png
  android: true

flutter:
  uses-material-design: true